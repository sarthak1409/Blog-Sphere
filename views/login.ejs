<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="/images/png/logo-grayscale-inverted.png">
    <title>BlogSphere - Login</title>
    <link rel="stylesheet" href="/style2.css">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f9f9f9;
            font-family: Arial, sans-serif;
            margin: 0;
        }
        .card {
            border: 5px solid #231717;
            border-radius: 1rem;
            padding: 40px;
            width: 300px;
            text-align: center;
            box-shadow: 10px 4px 8px rgba(0, 0, 0, 0.8);
            background-color: white;
        }
        .card h2 {
            font-size: 1.8em;
            margin-bottom: 20px;
        }
        .card input {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: 2px solid #ccc;
            border-radius: 3rem;
            font-size: 1em;
        }
        .card button {
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 3rem;
            cursor: pointer;
            font-size: 1.1em;
        }
        .card button:hover {
            background-color: #0056b3;
        }
        .card p {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="card">
        <h2>Login to BlogSphere</h2>
        <form action="/user/login" method="post">
            <input type="text" name="username" placeholder="Enter correct username" required>
            <input type="password" name="password" placeholder="Enter your password" required>
            <button type="submit">Login</button>
        </form>
        <p>Don't have an account? <a href="/user/register">Sign Up</a></p>
    </div>
</body>
</html>
